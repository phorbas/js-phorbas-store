export const _ident = v => v

export function _rec_set_key(rec, key) ::
  rec[2] ??= rec[0]
  rec[0] = key
  return rec

export function _decode_key_batch(batch) ::
  for let rec of batch ::
    if rec.length > 2 ::
      rec[0] = rec[2]
      rec.length = 2
  return batch

