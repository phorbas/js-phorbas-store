export async function _batch_await_inline(batch) ::
  for let k in batch ::
    batch[k] = await batch[k]
  return batch

export function _decode_key_batch(batch) ::
  for let rec of batch ::
    if rec.length > 2 ::
      rec[0] = rec[2]
      rec.length = 2
  return batch

